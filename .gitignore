# 凭据文件（600 权限，禁止入库）
.secrets.env

# 真实配置（含 worker_secret，S-P1-2 裁决）；示例文件 config.example.json 入库
config.json
config.json.bak

# 构建产物与 SQLite 运行时文件
/target
*.db
*.db-wal
*.db-shm

# clippy 独立 target 目录（工具链错配环境临时产物）
/target-clippy
.pproxy.env

# M5 desktop（独立构建图）
desktop/node_modules/
desktop/dist/
desktop/src-tauri/target/
desktop/src-tauri/gen/

# M6 gate worker 本地依赖
deploy/cf-gate-worker/node_modules/
deploy/render-gate/node_modules/

# 2026-08 安全审计整改：本地密钥与运行时/构建产物禁止入库
.dev.vars
.dev.vars.*
!.dev.vars.example
.wrangler/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db-journal
.vercel/
.env.local

# 根目录与 scripts 的依赖目录
/node_modules/
/scripts/node_modules/

# 根目录 node_modules 符号链接（git 视为文件，带斜杠的目录规则无法匹配）
/node_modules

# AI 工具本地状态（不入库）
.workbuddy/

# 开源前补充：本机运行时快照与本地 LLM 进程文件（git add . 防误提交）
telemetry-snapshot.json
ponyllm.pid
ponyllm-serve.log
ponyllm.toml.bak
desktop/src-tauri/tauri.updater.json
