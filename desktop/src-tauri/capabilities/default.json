{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "pony-desktop 授权：通知 + http（scope 通配放行任意 http/https 主机与端口——个人工具威胁模型：无第三方内容渲染、目标地址仅来自用户设置项；CSP connect-src 'none' 未放宽。语法依据：URLPattern 构造串省略 port 仅匹配默认端口，须显式 :* 才通配含默认端口在内全部端口——tauri#12734 维护者确认、plugins-workspace#2131、tauri-plugin-http 2.5.9 scope.rs 仅对 pathname/search/hash 补 *）+ 自定义凭据命令",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "notification:default",
    {
      "identifier": "http:default",
      "allow": [
        {
          "url": "http://*:*"
        },
        {
          "url": "https://*:*"
        }
      ]
    },
    "updater:default",
    "process:allow-restart"
  ]
}
